import_pdf_as_psd_config = {
    // Modify config options here.

    // Color Mode used when converting PDF to PSD
    color_mode: "RGB",
    // DPI used when converting PDF to PSD
    dpi_res: 72,
    // Use anti-alias when converting PDF to PSD
    anti_alias: true
}