## How to run tests.

* Use VSCode and install the recommended extensions. 
* Open the tests you want to run. 
* From the command palette menu select Adobe Indesign.
* Tests will run and output logs under `/tests/logs/`
* Check logs to see if tests passed or failed.